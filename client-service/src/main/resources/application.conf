mongo {
  host = "localhost"
  host = ${?MONGO_HOST}

  port = 32768
  port = ${?MONGO_PORT}

  db = "client"
  db = ${?MONGO_DB}
}

discovery {
  host = "localhost"
  host = ${?DISCOVERY_HOST}
  
  port = 35028
  port = ${?DISCOVERY_PORT}
}

service {
  restPort = 8080
  grpcPort = 35027
}