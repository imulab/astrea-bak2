spring.profiles.active: dummy

server:
  port: ${proxy.port:8080}

client:
  host: ${client.service.host:localhost}
  port: ${client.service.port:35027}

zuul:
  routes:
    tokenEndpoint:
      path: /
      url: http://${token.endpoint.host:localhost}:${token.endpoint.port:8080}/oauth/token

ribbon:
  okhttp:
    enabled: true